<% if @metric.id %>
  <%= link_to 'Delete', send(model_path(@metric), @metric),
                            class: 'btn btn-delete',
                            method: :delete,
                            data: {
                              confirm: 'Sure you want to delete this metric?'
                            } %>

<% end %>
