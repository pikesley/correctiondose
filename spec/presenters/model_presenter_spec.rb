describe ModelPresenter do
  before :each do
    DatabaseCleaner.clean
  end

  describe '#as_add_button' do
    it 'generate an add_new button' do
    #  expect(desc).to eq '<a class="btn btn-glucose" href="/glucose/new">Add Glucose</a>'
    end
  end
end
