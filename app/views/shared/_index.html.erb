<div class='row'>
  <div class='col-xs-6'>
    <h1><%= @model.short_name %></h1>
  </div>
  <div class='col-xs-6'>
    <%= link_to "Add #{@model.short_name}",
        send("new_#{@model.new.presenter.underscore}_path".to_sym),
        class: "btn #{@model.new.presenter.button_name} pull-right" %>
  </div>
</div>

<% unless @no_picker %>
  <%= render partial: 'shared/hours_picker', locals: {
    label: 'Charts',
    fontawesome: 'fa-area-chart',
    path: nil
  }%>
<% end %>

<%= render partial: 'shared/table', locals: {
  metrics: @metrics,
  with_year: true
} %>
